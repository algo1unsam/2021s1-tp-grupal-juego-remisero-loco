import auto.*
import obstaculos.*
import config.*
import extras.*
import wollok.game.*

program remiseroLocoGame {
	game.title("El remisero loco")
	game.height(10)
	game.width(8)
	
	game.boardGround("background.jpg")

	mainGame.iniciar()

	game.showAttributes(autoRojo) //Debug
	game.showAttributes(obstaculo)
	game.start()
}
